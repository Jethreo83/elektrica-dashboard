# .env.example — copy to .env and fill in real values. Never commit .env.

# Backend API base URL this frontend talks to. Backend runs on port
# 8001 (not FastAPI's default 8000 -- avoids a collision with the
# Complete Collision dashboard's backend, which also runs locally).
VITE_API_BASE_URL=http://localhost:8001
